#include <bits/stdc++.h>

using namespace std;

int main() {
    int x1 , y1 , x2 , y2;
    cin >> x1 >> y1 >> x2 >> y2;
    cout <<(float) (x1 + x2) / 2 << " " << (float)(y1 + y2) / 2 << "\n";
    return 0;
}
